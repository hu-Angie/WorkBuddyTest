# 实时协作工作台 · 使用说明

一个「主前端 + 轻量实时后端」的协作产品：任务看板 + 即时消息（评论与 @提醒）+ 实时活动流 + 在线成员同步。
前端负责全部交互体验，后端只做三件事：**实时广播、数据持久化、在线成员管理**。

---

## 一、快速开始

### 1. 环境要求
- Node.js 18 及以上（本项目在 Node 22 验证通过）

### 2. 安装依赖
```bash
npm install
```
只依赖两个包：`express`（Web 服务与静态托管）、`ws`（WebSocket 实时通信）。

### 3. 启动服务
```bash
node server.js
```
看到 `实时协作工作台已启动: http://localhost:3000` 即启动成功。

### 4. 打开页面
浏览器访问 **http://localhost:3000** ，首次进入会要求输入昵称（昵称会保存在浏览器本地，下次免输入）。

### 5. 换个端口（可选）
默认 3000，若被占用可用环境变量指定：

```bash
# Windows PowerShell
$env:PORT=4000; node server.js

# Windows CMD
set PORT=4000 && node server.js

# macOS / Linux
PORT=4000 node server.js
```

---

## 二、界面导览

| 区域 | 说明 |
|---|---|
| 顶部栏 | 项目名、在线成员头像组、在线人数、当前昵称（点击可改）、连接状态灯 |
| 看板区（左） | 三列：待办 / 进行中 / 已完成，卡片可拖拽 |
| 活动流（右） | 实时滚动所有人的操作记录，最新一条高亮；右侧绿灯表示 WebSocket 已连接 |

---

## 三、功能用法

### 1. 新建任务
点击任意列底部的 **「+ 添加任务」**，出现输入框，输入标题后按 **回车** 即创建（默认落到当前列）。

### 2. 拖拽换状态
按住卡片拖到另一列松开，任务状态自动更新，并实时广播给所有在线的人。

### 3. 编辑 / 删除任务
点击卡片打开详情面板，可修改：

- 标题、描述
- 负责人（留空显示为「未分配」）
- 优先级（高 / 中 / 低，对应卡片左侧色条）
- 状态（也可在此直接切换，等价于拖拽）
- 截止日期

点 **保存** 提交，点 **删除** 移除任务（会二次确认，并级联删除该任务的全部评论）。

### 4. 评论与 @提醒
在详情面板底部输入评论。要点：

- 输入 `@昵称` 即可提醒对方，面板下方有在线成员胶囊，**点击一下就自动插入 `@昵称`**，不必手打。
- 被 @ 的人看到的该条评论和活动流会**高亮显示为红色**，活动流条目前缀 `[提到你]`。
- 卡片上会显示该任务的评论条数。

### 5. 在线成员与实时同步
- 顶部头像组实时反映谁在线；有人进入或离开，活动流会提示。
- 任何人做的增删改、移动、评论，都会**秒级同步**到所有人的页面，无需刷新。
- 网络断开时右上角连接灯变灰，会自动每 2 秒重连，恢复后重新拉取全量状态。

### 6. 修改昵称
点击顶部昵称按钮 → 输入新昵称 → 页面刷新后生效。

---

## 四、怎么体验多人协作

在一台机器上就能试：

1. 打开多个浏览器标签页（或多个浏览器窗口），都访问 `http://localhost:3000`；
2. 每个标签页输入**不同昵称**（例如 小明 / 小红）；
3. 在 A 标签拖动卡片、发评论 `@小红`，B 标签会立刻看到看板变化和红色高亮的 @提醒。

若要在局域网内让同事一起访问，把 `localhost` 换成你的内网 IP 即可（需放行对应端口）。

---

## 五、技术架构

```
workbuddyTest/
├── server.js            # 后端：Express + WebSocket + 持久化
├── package.json
├── public/              # 前端（原生 HTML/CSS/JS，零构建）
│   ├── index.html       # 页面骨架（三列看板 + 活动流 + 详情面板）
│   ├── styles.css       # light 主题样式
│   └── app.js           # 渲染、拖拽、详情、WebSocket 实时同步
└── data/                # 运行时生成的数据文件
    ├── tasks.json
    └── comments.json
```

**前端职责**：看板渲染、HTML5 拖拽、详情面板与评论、活动流展示、WebSocket 订阅与断线重连。
统一由服务端广播驱动 UI 更新（自身操作也走广播回传），保证多端视图一致。

**后端职责**：
- 静态托管 `public/`
- REST 接口处理任务与评论的增删改
- WebSocket 向所有客户端广播变更事件
- 维护在线成员表（连接即注册，断线自动移除）
- 解析评论中的 `@昵称` 为 `mentions`
- 防抖写盘（200ms 合并写入，避免高频操作反复落盘）

---

## 六、接口说明

### REST

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/state` | 拉取全量状态（任务、评论、活动流、在线成员） |
| POST | `/api/tasks` | 创建任务，body：`{title, desc, assignee, priority, due, status}` |
| PUT | `/api/tasks/:id` | 更新任务任意字段；若 `status` 变化，广播事件为 `task:moved` |
| DELETE | `/api/tasks/:id` | 删除任务，并级联删除其评论 |
| POST | `/api/tasks/:id/comments` | 发布评论，body：`{author, text}`，自动解析 `mentions` |

### WebSocket

客户端 → 服务端（连接后首先发送，用于注册成员）：

```json
{ "type": "hello", "name": "小明" }
```

服务端 → 客户端：

| 事件 | 载荷 | 触发时机 |
|---|---|---|
| `init` | `tasks` / `comments` / `activities` / `members` / `you` | 新成员注册成功，下发全量快照 |
| `task:created` | `task`, `activity` | 新建任务 |
| `task:moved` | `task`, `activity` | 任务状态变更（拖拽或改状态） |
| `task:updated` | `task`, `activity` | 任务字段更新 |
| `task:deleted` | `taskId`, `activity` | 删除任务 |
| `comment:added` | `comment`, `activity` | 新增评论 |
| `member:joined` / `member:left` | `members`, `activity` | 成员进入 / 离开 |

---

## 七、数据模型

```js
// 任务
Task    { id, title, desc, assignee, status, priority, due, createdAt, updatedAt, order }
// status: todo | doing | done     priority: high | med | low

// 评论
Comment { id, taskId, author, text, mentions[], createdAt }

// 活动流（仅内存，最近 50 条）
Activity{ id, type, actor, summary, createdAt }
```

---

## 八、数据与持久化

- **任务与评论**：持久化在 `data/tasks.json` 和 `data/comments.json`，重启服务后仍在。
- **活动流**：仅存内存，服务重启后清空（这是有意简化，减少无意义的落盘）。
- **在线成员**：纯内存，刷新页面即重新注册。
- **重置演示数据**：删掉 `data/` 目录下的两个 JSON 文件再重启，会恢复 3 条内置示例任务。

---

## 九、常见问题

**页面空白或活动流不动？**
看右上角连接灯是否绿色；若灰色说明 WebSocket 未连上，检查服务是否在运行、端口是否被占用。

**改了任务但别人没看到？**
确认对方页面连接灯为绿色；断线重连会自动重新拉取全量状态。

**端口被占用？**
按第一节第 5 条用 `PORT` 环境变量换端口。

**想清空所有数据重来？**
停止服务 → 删除 `data/` 目录 → 重新启动。

**生产环境能用吗？**
这是 vibe coding 演示项目：无鉴权、无用户体系、数据存 JSON 文件，适合本地或小团队内网试用，不建议直接暴露公网。

---

## 十、可以继续加的方向

- 活动流也落盘持久化
- 任务分配提醒：被 @ 或未分配任务时推送浏览器通知
- 任务搜索 / 按负责人筛选
- 简单的登录与权限
